{
  "name": "banking-api-node",
  "version": "1.0.0",
  "description": "API Bancaire - version Node.js (portage depuis Java/Spring Boot)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.19.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "supertest": "^7.0.0",
    "vitest": "^1.6.0"
  }
}
